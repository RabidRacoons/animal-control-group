name: animal-control-group

on:
  push:
    branches: [master]
    
  pull_request:
    branches: [master]

jobs:
 
          
  Test:
    runs-on: windows-latest
    #needs: [deploy]
    env:
      ANIMAL_ADOPTION_FUNCTIONAL_TEST_PATH: https://dev-animal-web-app.azurewebsites.net
    

    steps:
      - uses: actions/checkout@v2
    
      - name: test1
        run: dotnet test 'AnimalAdoption.Web.Portal.FunctionalTests\AnimalAdoption.Web.Portal.FunctionalTests.csproj' --verbosity normal

